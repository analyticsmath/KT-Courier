# Multi-store checkout

One cart can hold bounded store groups. One checkout has one recipient and one
address, produces one group per store, accepts one provider payment, then creates
one parent marketplace order and one child store order per group. Provider split
payments are not used.
