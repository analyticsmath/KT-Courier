# Entitlements

Paid cycles grant bounded non-cash entitlements. Usage records reserve,
consume, release, reverse, expire or revoke through a stable operation ID.
Remaining amount and quantity can never become negative; a source operation
cannot consume the same grant twice.
