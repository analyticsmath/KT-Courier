# PayFast recurring payments

The provider-neutral recurring interface composes the existing PayFast
configuration, form construction and ITN verification primitives. It does not
collect card data, write raw tokens, expose credentials or treat browser return
as authority. Token/subscription mutations are source-locked pending Phase 26.5
compatibility proof.
