# Phase 28 pagination and filtering

Operational lists are bounded to 100 records, use deterministic public references and opaque signed cursors. Every filter is explicitly allowlisted; arbitrary fields, relation expansion, arbitrary sorting and reporting exports are unsupported.
