# Phase 28 operations

Processor entry points default to `--dry-run`, require bounded `--limit`, construct the production composition, and do not fabricate candidates. Apply mode remains locked until Phase 30. Retention execution is also production locked.
