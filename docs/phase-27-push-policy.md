# Phase 27 push policy

Push requires user initiation, browser permission, authenticated ownership, encrypted endpoint persistence and a verified endpoint. Payloads contain only a safe title/body, reference and approved internal route. No provider is configured; the production behavior is `PUSH_PROVIDER_NOT_CONFIGURED`.
