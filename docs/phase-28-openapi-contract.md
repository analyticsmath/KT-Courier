# Phase 28 OpenAPI contract

The v1 OpenAPI 3.1.2 contract is available at `/api/openapi/v1.json`; safe discovery is at `/.well-known/api-catalog`. The document defines opaque bearer authentication, scopes, problems, pagination, idempotency, rates, and webhook signing.
