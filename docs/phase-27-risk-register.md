# Phase 27 risk register

Open risks: no approved external providers, no deployed endpoint-encryption key, legacy notification compatibility data and unexecuted PostgreSQL/provider callback validation. Each is fail-closed by the production lock or a not-configured adapter.
