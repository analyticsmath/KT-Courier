# Phase 28 reconciliation

Cases have typed API and webhook reasons. Allowed recovery is narrowly scoped: rescan, recompute state, retry projection/delivery, verify or disable endpoint, revoke a compromised credential, or rebuild a schedule. Generic resolve and manual success are unsupported.
