# Pickup handoff

The store creates a short-lived hashed six-digit pickup challenge only after an active accepted assignment exists. The authenticated driver, not store staff, submits the second factor. Verification records package/seal references and marks only merchant-to-driver handoff. It neither writes courier delivery completion nor marks the customer delivery complete.
