# Merchant acceptance

Store review requires exact active-store ownership, `store_orders.review`, and an explicit-DENY-aware permission check. Acceptance requires the frozen policy deadline, resolved quantities, coherent settlement evidence, active store and a bounded estimate. It is irreversible; later failure uses an issue, cancellation request or adjustment.
