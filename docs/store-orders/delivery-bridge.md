# Delivery bridge

One `MarketplaceStoreOrderDeliveryBridge` may reference one existing courier order. It uses the frozen delivery quote/version and injected Phase 6 creation authority, then lets existing Phase 7 dispatch own assignment. Store staff have no driver-assignment endpoint.
