# Preparation

Accepted orders move from `NOT_STARTED` to `PREPARING` and then `READY_FOR_HANDOFF`. Estimate changes must remain inside the frozen maximum and extension. Ready requires every line to be resolved; a generic status dropdown is intentionally absent from the store UI.
