# Phase 27 preferences and consent

Preferences are scoped by user, category and channel with digest and quiet-hour fields. Security/legal categories cannot be disabled. Marketing is disabled unless channel-specific consent is `GRANTED`; revocation creates suppression evidence. `Africa/Johannesburg` is the documented fallback timezone.
