# Storefront accessibility

The shop uses a skip link, semantic landmarks, one page heading, accessible
breadcrumbs, labelled search combobox/listbox, keyboard suggestion movement,
announced result counts, visible focus, labelled variant links, textual
availability and non-motion loading placeholders. Browser WCAG verification is
deferred to Phase 26.5.

