# Storefront facets

Universal category, brand, store, availability, condition and fulfilment facets
are generated from public documents. Dynamic facets are restricted to the
approved Phase 18 `searchFacetSchema`; user-supplied field names are never used
as database filters. The canonical URL parser bounds parameter, facet and value
counts and preserves selected zero-count values.

