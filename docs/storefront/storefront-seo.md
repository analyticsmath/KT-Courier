# Storefront SEO

Product, category, store and indexable editorial collection URLs have canonical
policies. Search, filters, sorting, location, preview and arbitrary query URLs
are `noindex, follow`. Product-group/variant structured data is emitted only from
the exact visible price and availability. Segmented shop sitemaps return no
catalog URLs while the source lock is active.

