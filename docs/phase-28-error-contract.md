# Phase 28 error contract

Errors are `application/problem+json` with `type`, `title`, `status`, `detail`, `instance`, `code`, and `requestId`. Details never disclose credential existence, database models, stack traces, SQL, secrets, or resource ownership information.
