# Phase 27 reconciliation

Cases cover unmapped events, recipient failure, unsafe content, provider configuration, stalled delivery, receipt mismatch and invalid endpoints. Recovery actions are narrow: rescan, re-intake, re-fanout, retry eligible delivery, refresh receipt, deactivate endpoint or rebuild digest. There is no generic resolve.
