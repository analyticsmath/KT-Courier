# R20 illustration record

R20 reuses the R13 `RouteQueueIllustration` for an empty courier-order attention queue. It is decorative, contains no text or data, uses the existing mineral/oxide palette, and appears at most once in an empty state. No new R20 illustration asset is introduced.

No illustration appears inside operational tables, in a map substitute, for a locked commerce state, or as a claim of live operational status. No gradient, purple, glossy 3D, embedded text, fake chart, or private information is used.

The R20 commerce closure introduces no additional illustration. Catalog/storefront empty states and locked marketplace checkout rely on the existing protected state composition and textual lock explanation.
