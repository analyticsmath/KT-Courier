# Phase 27 testing

Focused DB-free tests cover consent, destination verification, restricted-content handling, strict rendering, retry bounds, unsubscribe signatures and production composition. PostgreSQL/provider/browser validation remains Phase 30 work.
