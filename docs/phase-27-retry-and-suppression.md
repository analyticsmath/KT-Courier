# Phase 27 retry and suppression

Retry is bounded to five attempts with exponential backoff and never retries configuration/authentication failure. Hard bounces, complaints, opt-outs, invalid endpoints and revocations create suppression evidence. Provider acceptance is not delivery.
