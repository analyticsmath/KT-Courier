import { defineConfig } from "vitest/config"; import tsconfigPaths from "vite-tsconfig-paths"; export default defineConfig({ plugins: [tsconfigPaths()], test: { environment: "node", include: ["tests/integration/promoter-*.integration.test.ts"], exclude: [], env: { PHASE25_DISPOSABLE_DATABASE_URL: process.env.PHASE25_DISPOSABLE_DATABASE_URL ?? "DISPOSABLE_PHASE25_DATABASE_ONLY" } } });
