import { describe, it, expect, beforeEach } from "vitest"; import { createDisposablePhase25Scenario } from "./phase25-scenario-support"; let scenario: ReturnType<typeof createDisposablePhase25Scenario>; describe("promoter earning PostgreSQL integration", () => { beforeEach(() => { scenario = createDisposablePhase25Scenario("earning", "Phase 14 and Phase 9 authority"); }); it("composes Phase 14 accrual and Phase 9 journal before release", async () => { const result = await scenario.execute("accrue-hold-journal-release"); expect(result).toMatchObject({ expected: "Phase 14 and Phase 9 authority", setup: { isolated: true } }); }); });
