import { describe, it, expect, beforeEach } from "vitest"; import { createDisposablePhase25Scenario } from "./phase25-scenario-support"; let scenario: ReturnType<typeof createDisposablePhase25Scenario>; describe("promoter qualification PostgreSQL integration", () => { beforeEach(() => { scenario = createDisposablePhase25Scenario("qualification", "settled evidence hold"); }); it("observes settled customer and store evidence then holds qualification", async () => { const result = await scenario.execute("observe-confirm-hold-invalidate"); expect(result).toMatchObject({ expected: "settled evidence hold", setup: { isolated: true } }); }); });
