import { describe, it, expect, beforeEach } from "vitest"; import { createDisposablePhase25Scenario } from "./phase25-scenario-support"; let scenario: ReturnType<typeof createDisposablePhase25Scenario>; describe("promoter program PostgreSQL integration", () => { beforeEach(() => { scenario = createDisposablePhase25Scenario("program", "immutable version lifecycle"); }); it("creates immutable versions and enrols an active eligible promoter", async () => { const result = await scenario.execute("draft-submit-approve-activate-enrol"); expect(result).toMatchObject({ expected: "immutable version lifecycle", setup: { isolated: true } }); }); });
