import { describe, it, expect, beforeEach } from "vitest"; import { createDisposablePhase25Scenario } from "./phase25-scenario-support"; let scenario: ReturnType<typeof createDisposablePhase25Scenario>; describe("promoter attribution PostgreSQL integration", () => { beforeEach(() => { scenario = createDisposablePhase25Scenario("attribution", "first valid touch wins"); }); it("binds one pre-registration first-valid touch and rejects overwrite", async () => { const result = await scenario.execute("bind-first-touch-reject-overwrite"); expect(result).toMatchObject({ expected: "first valid touch wins", setup: { isolated: true } }); }); });
