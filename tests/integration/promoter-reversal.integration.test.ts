import { describe, it, expect, beforeEach } from "vitest"; import { createDisposablePhase25Scenario } from "./phase25-scenario-support"; let scenario: ReturnType<typeof createDisposablePhase25Scenario>; describe("promoter reversal PostgreSQL integration", () => { beforeEach(() => { scenario = createDisposablePhase25Scenario("reversal", "bounded reversal ceiling"); }); it("reverses no more than canonical earning evidence after fraud or refund", async () => { const result = await scenario.execute("refund-chargeback-fraud-reverse"); expect(result).toMatchObject({ expected: "bounded reversal ceiling", setup: { isolated: true } }); }); });
