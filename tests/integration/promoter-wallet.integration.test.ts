import { describe, it, expect, beforeEach } from "vitest"; import { createDisposablePhase25Scenario } from "./phase25-scenario-support"; let scenario: ReturnType<typeof createDisposablePhase25Scenario>; describe("promoter wallet PostgreSQL integration", () => { beforeEach(() => { scenario = createDisposablePhase25Scenario("wallet", "Phase 9 and Phase 13 authority"); }); it("projects Phase 9 funds and requests Phase 13 withdrawal without direct credit", async () => { const result = await scenario.execute("project-wallet-request-withdrawal"); expect(result).toMatchObject({ expected: "Phase 9 and Phase 13 authority", setup: { isolated: true } }); }); });
