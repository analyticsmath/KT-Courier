import { test, expect } from "@playwright/test"; test.beforeEach(async ({ page }) => { await page.goto("/promoter/referrals"); }); test.skip("promoter referrals distinguishes touches, attribution and qualification without customer PII", async ({ page }) => { await page.locator("main").waitFor(); await expect(page.locator("main")).toBeVisible(); await expect(page.locator("body")).not.toContainText(/customer@example|phone|address/i); });
