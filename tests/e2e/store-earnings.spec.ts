import { test } from "@playwright/test";

test.describe("store earning owner flows", () => {
  test.skip("shows the exact Earnings heading, payable balance, accrual list, and release eligibility", async () => {});
  test.skip("shows refund-reserved, released amounts, and safe detail history as exact ZAR strings", async () => {});
  test.skip("denies cross-store detail access without revealing whether the earning exists", async () => {});
  test.skip("exposes no mutation control, customer data, wallet ID, or payable account ID", async () => {});
});
