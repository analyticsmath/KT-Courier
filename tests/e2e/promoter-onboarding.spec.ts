import { test, expect } from "@playwright/test"; test.beforeEach(async ({ page }) => { await page.goto("/promoter"); }); test.skip("promoter onboarding shows application, agreement, compliance, lock and denied states", async ({ page }) => { await page.locator("main").waitFor(); await expect(page).toHaveTitle(/KT/i); await expect(page.locator("body")).toContainText(/lock|permission|safe/i); });
