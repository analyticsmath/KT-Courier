import { test, expect } from "@playwright/test"; test.beforeEach(async ({ page }) => { await page.goto("/promoter/earnings"); }); test.skip("promoter earnings distinguishes held, payable, withdrawn and reversed evidence", async ({ page }) => { await page.locator("main").waitFor(); await expect(page.locator("main")).toBeVisible(); await expect(page.locator("body")).toContainText(/held|payable|reversed/i); });
