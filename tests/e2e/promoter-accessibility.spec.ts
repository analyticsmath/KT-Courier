import { test, expect } from "@playwright/test"; test.beforeEach(async ({ page }) => { await page.goto("/promoter"); }); test.skip("promoter screens expose responsive live status and accessible navigation", async ({ page }) => { await page.locator("nav").waitFor(); await expect(page.locator("[aria-live]")).toBeVisible(); await expect(page.locator("nav")).toHaveAttribute("aria-label", /.+/); });
