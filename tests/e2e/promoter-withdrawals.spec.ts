import { test, expect } from "@playwright/test"; test.beforeEach(async ({ page }) => { await page.goto("/promoter/withdrawals"); }); test.skip("promoter withdrawal is production-locked and never directly credits funds", async ({ page }) => { await page.locator("main").waitFor(); await expect(page.locator("main")).toBeVisible(); await expect(page.locator("body")).toContainText(/locked|withdraw/i); });
