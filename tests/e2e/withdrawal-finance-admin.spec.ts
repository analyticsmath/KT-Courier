import { test } from "@playwright/test";
test.describe("finance withdrawals", () => { test.skip("covers overview, review, approval, separate payout processor, reconciliation, destinations, DENY, and privacy", async () => {}); });
