import { test, expect } from "@playwright/test"; test.beforeEach(async ({ page }) => { await page.goto("/promoter/disputes"); }); test.skip("promoter dispute supports safe evidence and reconciliation state", async ({ page }) => { await page.locator("main").waitFor(); await expect(page.locator("main")).toBeVisible(); await expect(page.locator("body")).not.toContainText(/customer@example|payment details/i); });
