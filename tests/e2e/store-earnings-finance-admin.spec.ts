import { test } from "@playwright/test";

test.describe("store earning finance admin flows", () => {
  test.skip("shows the exact Store Earnings heading with list and detail evidence", async () => {});
  test.skip("shows commission attribution, ledger links, refund projections, and reconciliation", async () => {});
  test.skip("submits only an approved reversal reason and operation ID when reversal is valid", async () => {});
  test.skip("blocks reversal after release and honors explicit DENY", async () => {});
  test.skip("exposes no amount editor, balance adjustment, create-earning, or mark-release control", async () => {});
});
