import { test, expect } from "@playwright/test"; test.beforeEach(async ({ page }) => { await page.goto("/promoter/links"); }); test.skip("promoter links creates only masked secure codes and archives them", async ({ page }) => { await page.locator("main").waitFor(); await expect(page.locator("main")).toBeVisible(); await expect(page.locator("body")).not.toContainText(/HMAC|raw code/i); });
