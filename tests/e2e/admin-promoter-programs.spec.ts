import { test, expect } from "@playwright/test"; test.beforeEach(async ({ page }) => { await page.goto("/admin/promoter-programs"); }); test.skip("admin program lifecycle rejects unavailable business acquisition", async ({ page }) => { await page.getByRole("main").waitFor(); await expect(page.locator("main")).toBeVisible(); await expect(page.locator("body")).not.toContainText("BusinessAccount"); });
