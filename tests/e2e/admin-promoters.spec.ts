import { test, expect } from "@playwright/test"; test.beforeEach(async ({ page }) => { await page.goto("/admin/promoters"); }); test.skip("admin promoter detail projects lifecycle, compliance, evidence and audit history", async ({ page }) => { await page.getByRole("main").waitFor(); await expect(page.locator("main")).toBeVisible(); await expect(page.locator("body")).toContainText(/promoter/i); });
