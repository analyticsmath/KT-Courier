import { describe, expect, it } from "vitest";
import { assertCatalogMediaDeclaration, assertCatalogMediaDimensions, assertCatalogMediaCounts, assertCatalogMediaPurposeForOwner, assertCatalogMediaPurposeRole, CATALOG_MEDIA_MAX_UPLOAD_BYTES } from "@/lib/catalog/media/catalog-media-policy";

describe("catalog media policy", () => {
  it("allows only bounded JPEG PNG and WebP images", () => { expect(() => assertCatalogMediaDeclaration({ purpose: "PRODUCT_IMAGE", declaredMimeType: "image/png", declaredByteSize: CATALOG_MEDIA_MAX_UPLOAD_BYTES })).not.toThrow(); expect(() => assertCatalogMediaDeclaration({ purpose: "PRODUCT_IMAGE", declaredMimeType: "image/svg+xml", declaredByteSize: 100 })).toThrow(); expect(() => assertCatalogMediaDeclaration({ purpose: "PRODUCT_IMAGE", declaredMimeType: "image/png", declaredByteSize: CATALOG_MEDIA_MAX_UPLOAD_BYTES + 1 })).toThrow(); });
  it("bounds dimensions pixels aspect ratio and counts", () => { expect(() => assertCatalogMediaDimensions(400, 400)).not.toThrow(); expect(() => assertCatalogMediaDimensions(10_000, 10_000)).toThrow(); expect(() => assertCatalogMediaDimensions(300, 3_000)).toThrow(); expect(() => assertCatalogMediaCounts({ productImageCount: 12, variantImageCount: 6, primaryImageCount: 1 })).not.toThrow(); expect(() => assertCatalogMediaCounts({ productImageCount: 13, variantImageCount: 0, primaryImageCount: 1 })).toThrow(); });
  it("keeps store purposes and attachment roles explicit", () => { expect(() => assertCatalogMediaPurposeForOwner("STORE", "PRODUCT_IMAGE")).not.toThrow(); expect(() => assertCatalogMediaPurposeForOwner("STORE", "CATEGORY_IMAGE")).toThrow(); expect(() => assertCatalogMediaPurposeRole("PRODUCT_IMAGE", "PRIMARY")).not.toThrow(); expect(() => assertCatalogMediaPurposeRole("PRODUCT_IMAGE", "VARIANT")).toThrow(); });
});
