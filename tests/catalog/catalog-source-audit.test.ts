import { readFileSync } from "node:fs"; import path from "node:path"; import { describe, expect, it } from "vitest";
const root=process.cwd(); const lock=readFileSync(path.join(root,"lib/catalog/catalog-production-lock.ts"),"utf8"); const migration=readFileSync(path.join(root,"prisma/migrations/20260717100000_phase18_product_catalog/migration.sql"),"utf8");
describe("catalog source audit",()=>{it("keeps production lock literal and env independent",()=>{expect(lock).toMatch(/CATALOG_PRODUCTION_VALIDATION_APPROVED\s*=\s*false/);expect(lock).not.toMatch(/process\.env/)});it("preserves append-only evidence and price overlap controls",()=>{expect(migration).toMatch(/CatalogInventoryMovement_immutable/);expect(migration).toMatch(/StoreOfferPriceVersion_no_overlap/);expect(migration).toMatch(/CatalogCategory_guard/)});it("does not modify Phase 19 commerce",()=>{const routes=readFileSync(path.join(root,"lib/catalog/catalog-route-handlers.ts"),"utf8");expect(routes).not.toMatch(/prisma\.(cart|order|payment|ledger|storeEarning|driverEarning)\.(create|update|upsert|delete)/)});});

