import { ProtectedPageHeader } from "@/components/protected-v2/surfaces/ProtectedPageHeader";import { ProtectedState } from "@/components/protected-v2/feedback/ProtectedState";import { getApplicantContext } from "@/lib/applicant-presentation/applicant-data";
export default async function ProfilePage(){const {profile}=await getApplicantContext();return <><ProtectedPageHeader eyebrow="Candidate dossier" title="Applicant profile" description="Profile changes remain subject to the existing profile service."/>{!profile?<ProtectedState className="mt-6" kind="empty" title="No applicant profile" description="The current workspace does not expose a verified profile-creation form without duplicating identity and eligibility validation."/>:<section className="eo-panel eo-panel--default mt-6"><h2 className="eo-panel__title">Profile recorded</h2><p className="eo-panel__description">Your applicant profile is connected to this private workspace. Sensitive profile values are not repeated in this overview.</p></section>}</>}
