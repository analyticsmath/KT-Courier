/* eslint-disable @typescript-eslint/no-explicit-any */
import type { NextRequest } from "next/server"; import { PERMISSIONS } from "@/lib/auth/permission-keys"; import { requirePromoterRead } from "@/lib/promoters/api-policy"; import { db, promoterJson, safeRows } from "@/lib/promoters/route-support";
export async function GET(request: NextRequest) { const auth = await requirePromoterRead(PERMISSIONS.PROMOTER_REFERRALS_READ_OWN, request, "/api/promoter/referrals"); if ("response" in auth) return auth.response; const rows = await db.promoterAttribution.findMany({ where: { promoterAccountId: auth.account.id }, include: { qualifications: { select: { publicReference: true, status: true, holdUntil: true } }, touch: { select: { publicReference: true, occurredAt: true, destinationType: true } } }, orderBy: { createdAt: "desc" } }); return promoterJson({ referrals: safeRows(rows as any) }); }
