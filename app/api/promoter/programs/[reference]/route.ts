import type { NextRequest } from "next/server";
import { PERMISSIONS } from "@/lib/auth/permission-keys"; import { requirePromoterRead } from "@/lib/promoters/api-policy"; import { db, promoterJson, safeRows } from "@/lib/promoters/route-support";
export async function GET(request: NextRequest, context: { params: Promise<{ reference: string }> }) { const auth = await requirePromoterRead(PERMISSIONS.PROMOTER_PROGRAMS_READ, request, "/api/promoter/programs/[reference]"); if ("response" in auth) return auth.response; const { reference } = await context.params; const version = await db.promoterProgramVersion.findFirst({ where: { publicReference: reference, status: "ACTIVE", program: { status: "ACTIVE" } }, include: { program: true, enrollments: { where: { promoterAccountId: auth.account.id }, select: { publicReference: true, status: true } } } }); return version ? promoterJson({ program: safeRows([version])[0] }) : promoterJson({ error: "Program was not found." }, 404); }
