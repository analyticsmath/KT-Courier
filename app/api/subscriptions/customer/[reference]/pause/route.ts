import type { NextRequest } from "next/server"; import { enforceSubscriptionMutation, requireSubscriptionCustomer } from "@/lib/subscriptions/api-policy"; import { subscriptionMutationSourceLocked } from "@/lib/subscriptions/locked-mutation-route"; export async function POST(request: NextRequest) { const limited = await enforceSubscriptionMutation(request); if (limited) return limited; const auth = await requireSubscriptionCustomer(request); return auth.response ?? subscriptionMutationSourceLocked(); }
