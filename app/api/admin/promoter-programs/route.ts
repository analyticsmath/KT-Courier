import type { NextRequest } from "next/server"; import { PERMISSIONS } from "@/lib/auth/permission-keys"; import { promoterAdminCollection } from "@/lib/promoters/admin-read"; import { createProgram } from "@/lib/promoters/admin-lifecycle-route"; export const GET = (request: NextRequest) => promoterAdminCollection(request, PERMISSIONS.PROMOTER_PROGRAMS_MANAGE, "promoter-programs"); export const POST = createProgram;
