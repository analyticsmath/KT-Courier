import type { NextRequest } from "next/server"; import { PERMISSIONS } from "@/lib/auth/permission-keys"; import { promoterAdminDetail } from "@/lib/promoters/admin-read"; import { updateProgram } from "@/lib/promoters/admin-lifecycle-route"; export async function GET(request: NextRequest, context: { params: Promise<{ reference: string }> }) { return promoterAdminDetail(request, PERMISSIONS.PROMOTER_PROGRAMS_MANAGE, "promoter-programs", (await context.params).reference); } export const PATCH = updateProgram;
