/* eslint-disable @typescript-eslint/no-explicit-any -- Prisma client generation is intentionally deferred. */
import { NextResponse } from "next/server"; import { PERMISSIONS } from "@/lib/auth/permission-keys"; import { prisma } from "@/lib/db/prisma"; import { notificationAdminAccess } from "@/lib/notifications/admin-api";
export async function GET(request: Request, context: RouteContext<"/api/admin/notifications/reconciliation/[reference]">) { const access = await notificationAdminAccess(request, PERMISSIONS.NOTIFICATION_RECONCILIATION_READ); if ("response" in access) return access.response; const { reference } = await context.params; const item = await (prisma as any).notificationReconciliationCase.findUnique({ where: { publicReference: reference } }); return item ? NextResponse.json({ data: item }) : NextResponse.json({ error: "Reconciliation case not found." }, { status: 404 }); }
