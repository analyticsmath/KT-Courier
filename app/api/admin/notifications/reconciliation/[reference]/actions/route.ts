import { NextResponse } from "next/server"; import { z } from "zod"; import { PERMISSIONS } from "@/lib/auth/permission-keys"; import { notificationAdminAccess, notificationFailure, parseNotificationBody } from "@/lib/notifications/admin-api";
const body = z.object({ action: z.enum(["rescan", "retry-source-intake", "retry-fan-out", "retry-delivery", "refresh-provider-receipt", "deactivate-invalid-endpoint", "rebuild-digest"]) });
export async function POST(request: Request, context: RouteContext<"/api/admin/notifications/reconciliation/[reference]/actions">) { const access = await notificationAdminAccess(request, PERMISSIONS.NOTIFICATION_RECONCILIATION_RETRY, true); if ("response" in access) return access.response; const parsed = await parseNotificationBody(request, body); if ("response" in parsed) return parsed.response; const { reference } = await context.params; try { return NextResponse.json({ data: await access.authority.reconciliation.act(reference, parsed.data.action) }); } catch (error) { return notificationFailure(error); } }
