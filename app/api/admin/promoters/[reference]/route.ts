import type { NextRequest } from "next/server"; import { PERMISSIONS } from "@/lib/auth/permission-keys"; import { promoterAdminDetail } from "@/lib/promoters/admin-read"; export async function GET(request: NextRequest, context: { params: Promise<{ reference: string }> }) { return promoterAdminDetail(request, PERMISSIONS.PROMOTERS_READ, "promoters", (await context.params).reference); }
