/* eslint-disable @typescript-eslint/no-explicit-any -- Phase 25 delegates are generated during deferred validation. */
import type { NextRequest } from "next/server"; import { prisma } from "@/lib/db/prisma"; import { PERMISSIONS } from "@/lib/auth/permission-keys"; import { requirePromoterAdmin } from "@/lib/promoters/admin-api-policy"; import { confirmPromoterFraud } from "@/lib/promoters/promoter-fraud.service";
export async function POST(request: NextRequest, context: { params: Promise<{ reference: string }> }) { const auth = await requirePromoterAdmin(request, PERMISSIONS.PROMOTER_FRAUD_MANAGE, "/api/admin/promoter-fraud/[reference]/confirm", true); if ("response" in auth) return auth.response; const { reference } = await context.params; try { return Response.json({ case: await confirmPromoterFraud(prisma as any, { reference, ...(await request.json()) }) }); } catch { return Response.json({ error: "Fraud confirmation was rejected." }, { status: 422 }); } }
