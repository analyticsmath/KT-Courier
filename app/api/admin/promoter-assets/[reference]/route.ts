import type { NextRequest } from "next/server"; import { PERMISSIONS } from "@/lib/auth/permission-keys"; import { promoterAdminDetail } from "@/lib/promoters/admin-read"; import { updateAsset } from "@/lib/promoters/admin-lifecycle-route"; export async function GET(request: NextRequest, context: { params: Promise<{ reference: string }> }) { return promoterAdminDetail(request, PERMISSIONS.PROMOTER_ASSETS_MANAGE, "promoter-assets", (await context.params).reference); } export const PATCH = updateAsset;
