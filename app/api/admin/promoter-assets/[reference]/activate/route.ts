import type { NextRequest } from "next/server"; import { assetAction } from "@/lib/promoters/admin-lifecycle-route"; export const POST = (r: NextRequest, c: { params: Promise<{ reference: string }> }) => assetAction(r, c, "activate");
