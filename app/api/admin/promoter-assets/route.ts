import type { NextRequest } from "next/server"; import { PERMISSIONS } from "@/lib/auth/permission-keys"; import { promoterAdminCollection } from "@/lib/promoters/admin-read"; import { createAsset } from "@/lib/promoters/admin-lifecycle-route"; export const GET = (request: NextRequest) => promoterAdminCollection(request, PERMISSIONS.PROMOTER_ASSETS_MANAGE, "promoter-assets"); export const POST = createAsset;
