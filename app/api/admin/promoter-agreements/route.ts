import type { NextRequest } from "next/server"; import { PERMISSIONS } from "@/lib/auth/permission-keys"; import { promoterAdminCollection } from "@/lib/promoters/admin-read"; import { createAgreement } from "@/lib/promoters/admin-lifecycle-route"; export const GET = (request: NextRequest) => promoterAdminCollection(request, PERMISSIONS.PROMOTER_AGREEMENTS_MANAGE, "promoter-agreements"); export const POST = createAgreement;
