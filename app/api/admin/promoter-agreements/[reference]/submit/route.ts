import type { NextRequest } from "next/server"; import { agreementAction } from "@/lib/promoters/admin-lifecycle-route"; export const POST = (r: NextRequest, c: { params: Promise<{ reference: string }> }) => agreementAction(r, c, "submit");
