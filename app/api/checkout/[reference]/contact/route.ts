import { type NextRequest } from "next/server";
import { updateMarketplaceCheckoutContact } from "@/lib/marketplace-checkout/checkout.service";
import { assertExactKeys, enforceMarketplaceMutation, integerField, marketplaceError, marketplaceJson, marketplaceOwner, readMarketplaceJson, stringField } from "@/lib/marketplace-checkout/api-policy";
export async function PUT(request: NextRequest, context: { params: Promise<{ reference: string }> }) { const limited = await enforceMarketplaceMutation(request, "checkout"); if (limited) return limited; try { const owner = await marketplaceOwner(request, "checkout"); if (!owner) return marketplaceJson({ error: "Checkout access is required." }, 401); const body = await readMarketplaceJson(request); assertExactKeys(body, ["recipientName", "email", "phone", "preferredContactMethod", "operationId", "requestHash", "checkoutVersion"]); const { reference } = await context.params; const checkout = await updateMarketplaceCheckoutContact({ reference, owner, operation: { operationId: stringField(body, "operationId", 160), requestHash: stringField(body, "requestHash", 160), expectedVersion: integerField(body, "checkoutVersion") }, contact: { recipientName: stringField(body, "recipientName", 120), email: stringField(body, "email", 254).toLowerCase(), phone: stringField(body, "phone", 32), preferredContactMethod: typeof body.preferredContactMethod === "string" ? body.preferredContactMethod.slice(0, 32) : undefined } }); return marketplaceJson({ checkout }); } catch (error) { return marketplaceError(error); } }
