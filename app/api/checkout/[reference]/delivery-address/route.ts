import { type NextRequest } from "next/server";
import { updateMarketplaceCheckoutAddress } from "@/lib/marketplace-checkout/checkout.service";
import { assertExactKeys, enforceMarketplaceMutation, integerField, marketplaceError, marketplaceJson, marketplaceOwner, readMarketplaceJson, stringField } from "@/lib/marketplace-checkout/api-policy";
export async function PUT(request: NextRequest, context: { params: Promise<{ reference: string }> }) { const limited = await enforceMarketplaceMutation(request, "checkout"); if (limited) return limited; try { const owner = await marketplaceOwner(request, "checkout"); if (!owner) return marketplaceJson({ error: "Checkout access is required." }, 401); const body = await readMarketplaceJson(request); assertExactKeys(body, ["recipientName", "line1", "line2", "suburb", "city", "province", "postalCode", "deliveryInstructions", "serviceAreaReference", "operationId", "requestHash", "checkoutVersion"]); const optional = (key: string, max: number) => body[key] === undefined ? undefined : stringField(body, key, max); const { reference } = await context.params; const checkout = await updateMarketplaceCheckoutAddress({ reference, owner, operation: { operationId: stringField(body, "operationId", 160), requestHash: stringField(body, "requestHash", 160), expectedVersion: integerField(body, "checkoutVersion") }, address: { recipientName: stringField(body, "recipientName", 120), line1: stringField(body, "line1", 180), line2: optional("line2", 180), suburb: optional("suburb", 100), city: stringField(body, "city", 100), province: stringField(body, "province", 100), postalCode: optional("postalCode", 24), deliveryInstructions: optional("deliveryInstructions", 500), serviceAreaReference: optional("serviceAreaReference", 120) } }); return marketplaceJson({ checkout }); } catch (error) { return marketplaceError(error); } }
