import { runPromoterProcessor } from "./promoter-processor.mjs";
await runPromoterProcessor({ name: "phase25-promoter-preflight", selectCandidates: async (prisma, limit) => prisma.promoterAccount.findMany({ where: { status: { in: ["APPLIED", "APPROVED", "ACTIVE", "SUSPENDED"] } }, take: limit, select: { id: true, publicReference: true, identityStatus: true, taxProfileStatus: true, payoutReadinessStatus: true, agreementStatus: true } }), process: async ({ root, candidate, operationId }) => root.services.fraud.evaluatePromoterReleaseRisk({ identityReady: candidate.identityStatus === "VERIFIED", taxReady: candidate.taxProfileStatus === "READY", payoutReady: candidate.payoutReadinessStatus === "READY", agreementAccepted: candidate.agreementStatus === "ACCEPTED", operationId }) });
