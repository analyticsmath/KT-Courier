import { runPromoterProcessor } from "./promoter-processor.mjs";
await runPromoterProcessor({ name: "scan-promoter-reconciliation", selectCandidates: async (prisma, limit) => prisma.promoterAttribution.findMany({ take: limit, select: { id: true, publicReference: true, promoterAccountId: true, programVersionId: true, touchId: true, subjectKey: true, touch: { select: { validityStatus: true } } } }), process: async ({ prisma, root, candidate, operationId }) => { const duplicateAttribution = await prisma.promoterAttribution.count({ where: { programVersionId: candidate.programVersionId, subjectKey: candidate.subjectKey } }) > 1; return root.services.reconciliation.scanPromoterReconciliation(prisma, { ...candidate, comparison: { touchValid: candidate.touch?.validityStatus === "VALID", duplicateAttribution }, operationId }); } });
